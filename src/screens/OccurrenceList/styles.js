import styled from 'styled-components/native';
import {Text as TextTemplate} from 'react-native-paper';

export const ScreenView = styled.ScrollView``;

export const Text = styled(TextTemplate)`
  font-size: 18px;
`;
