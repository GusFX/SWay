import styled from 'styled-components/native';
import {default as DropdownTemplate} from 'react-native-paper-dropdown';

export const DropDown = styled(DropdownTemplate)``;

export const DropDownView = styled.View`
  margin-bottom: 5%;
`;
